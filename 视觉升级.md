# 日语单词背诵 App 视觉升级开发规范文档

## 文档基础信息

- 项目名称：日本語単語暗記 App
- 技术栈：React19 + TS + Vite + TailwindCSS3.4 + lucide-react
- 原有结构：路由、页面、组件、数据结构**完全保留**，仅做视觉 / 样式 / 动画升级，不改动业务逻辑、接口、状态逻辑
- 升级目标：统一现代柔和轻奢学习风，规范双模式色彩、组件、排版、动效，修复原有视觉割裂问题，提升精致感与可读性
- 适用对象：前端开发 AI / 开发人员，所有样式修改严格遵循本文档约束

# 一、升级核心设计原则（全局强制遵守）

1. **轻量化毛玻璃柔和风格**：全部卡片、弹窗、导航统一使用 `card-glass` 毛玻璃基底，减少高饱和纯色实底，降低视觉压迫
2. **色彩语义统一**：废弃散落在代码内的零散 Hex 色值，全部改用 CSS 变量语义 Token，Tailwind 扩展对应变量名
3. **8px 栅格统一**：内外边距、圆角、间距全部基于 8px 倍数，消除页面尺寸混乱
4. **双模式完全对称**：亮色 / 暗色模式一一对应，无遗漏组件、无色彩断层
5. **微交互标准化**：统一按压、hover、入场、切换动画时长与缓动函数，废弃旧版冲突交互类
6. **信息分层降噪**：弱化装饰元素，强化日语文字可读性，谚语、单词、语法文字层级清晰
7. **保留原有业务结构**：页面路由、组件拆分、接口请求、SSE 流式逻辑、单词滑动 / 闪卡翻转逻辑**不修改**，仅替换样式与动画实现

# 二、全局色彩系统（替换原有零散色值，CSS 变量 + Tailwind 扩展）

## 2.1 亮色模式 :root（Light）

css

```css
:root {
  /* 背景层 */
  --bg-root: #F8F6FF; /* 页面底层，替代旧#F5F3FF */
  --bg-card: #FFFFFF; /* 标准卡片白底 */
  --bg-surface: #F3EEFF; /* 次级面板、标签底色 */
  --bg-subtle: #FAF7FF; /* 极浅渐变背景、头部底层 */

  /* 主色品牌紫（统一柔和低饱和） */
  --primary-50: #F3EEFF;
  --primary-200: #D8C4FC;
  --primary-400: #A78BFA;
  --primary-600: #8B5CF6;
  --primary-800: #6D28D9;
  --primary-glow: rgba(139, 92, 246, 0.25);

  /* 功能辅助色（统一降低饱和度，柔和不刺眼） */
  --accent-orange: #F27044; /* 瞬間レビュー闪卡主题 */
  --orange-bg: #FFF1EC;
  --success-teal: #039496; /* 答对、完成、日历统计 */
  --teal-bg: #D1F3F2;
  --danger-red: #D13838; /* 错误、删除、周日标记 */
  --red-bg: #FDE8E8;

  /* 文字层级 */
  --text-main: #14161F; /* 标题、日语单词大字 */
  --text-sub: #424250; /* 副标题、释义、描述 */
  --text-hint: #818196; /* 辅助小字、提示、占位 */

  /* 分割/边框 */
  --border-light: rgba(139, 92, 246, 0.12);
  --border-strong: rgba(139, 92, 246, 0.22);
}
```

## 2.2 暗色模式 .dark（Dark）

css

```css
.dark {
  /* 背景层（分层加深，层级区分更强） */
  --bg-root: #0C0818; /* 底层，替代旧#0E0A1A */
  --bg-card: #1A1529; /* 卡片基底，统一废弃#1C1828/#1F1A2E两套色 */
  --bg-surface: #221C36; /* 标签、进度条底色 */
  --bg-subtle: #141024; /* 头部渐变底层 */

  /* 主色亮紫（适配暗色不发灰） */
  --primary-50: #DDD6FE;
  --primary-200: #C4B5FD;
  --primary-400: #A78BFA;
  --primary-600: #9061F9;
  --primary-800: #7C3AED;
  --primary-glow: rgba(167, 139, 250, 0.2);

  /* 功能辅助色暗色适配 */
  --accent-orange: #FF845A;
  --orange-bg: #362019;
  --success-teal: #27C3C6;
  --teal-bg: #143838;
  --danger-red: #F06060;
  --red-bg: #361A1A;

  /* 文字层级 */
  --text-main: #EAEAFF;
  --text-sub: #B0B0D0;
  --text-hint: #787899;

  /* 分割/边框（暗色半透紫边） */
  --border-light: rgba(167, 139, 250, 0.1);
  --border-strong: rgba(167, 139, 250, 0.2);
}
```

## 2.3 Tailwind 扩展配置（tailwind.config.ts）

将上述全部变量注册为自定义色，全局直接使用，禁止写死 Hex：

ts

```ts
theme: {
  extend: {
    colors: {
      root: "var(--bg-root)",
      card: "var(--bg-card)",
      surface: "var(--bg-surface)",
      subtle: "var(--bg-subtle)",
      primary: {
        50: "var(--primary-50)",
        200: "var(--primary-200)",
        400: "var(--primary-400)",
        600: "var(--primary-600)",
        800: "var(--primary-800)",
      },
      accent: "var(--accent-orange)",
      success: "var(--success-teal)",
      danger: "var(--danger-red)",
      orangeBg: "var(--orange-bg)",
      tealBg: "var(--teal-bg)",
      redBg: "var(--red-bg)",
    },
    textColor: {
      main: "var(--text-main)",
      sub: "var(--text-sub)",
      hint: "var(--text-hint)",
    },
    borderColor: {
      light: "var(--border-light)",
      strong: "var(--border-strong)",
    },
  },
}
```

## 2.4 色彩使用强制规范

1. 所有页面、组件**禁止直接写 #xxxxxx**，必须使用上述自定义颜色名
2. 主色 `primary-600`：主按钮、选中 Tab、进度条、高亮标签
3. `primary-400`：图标、次要高亮、导航选中图标
4. `surface`：标签、进度条背景、卡片内小模块底色
5. 橙色系列仅用于「瞬間レビュー闪卡」全页面，不混用其他模块
6. 青色仅用于完成、答对、学习统计正向数据
7. 红色仅用于错误、删除、周日日期标记

# 三、全局排版与字体规范（统一日语阅读体验）

## 3.1 字体栈（全局 index.css）

css

```css
* {
  font-family: "Noto Sans JP", "Noto Sans SC", system-ui, -apple-system, sans-serif;
  letter-spacing: 0.02em;
}
```

## 3.2 字号、字重、行高（固定层级，全页面统一）

表格

| 层级     | 字号 | 字重 | 行高 | 使用场景                         |
| :------- | :--- | :--- | :--- | :------------------------------- |
| 大标题   | 32px | 700  | 120% | 首页用户名、闪卡大字单词         |
| 页面标题 | 24px | 600  | 130% | 各页面顶部 Header 标题           |
| 模块标题 | 18px | 600  | 140% | 卡片标题、语法 Tab 标题          |
| 正文日语 | 16px | 400  | 160% | 单词、例句、AI 对话正文          |
| 辅助文字 | 14px | 400  | 150% | 释义、进度数字、列表描述         |
| 小字提示 | 12px | 400  | 140% | 标签、日期、底部说明、空状态提示 |

## 3.3 日语文字特殊规则

1. 汉字 + 假名组合统一字号，假名不缩小
2. 谚语、例句行高放宽至 1.7，提升阅读舒适度
3. 数字（学习天数、单词数量）统一 600 字重，突出数据

# 四、全局 CSS 工具类重构（废弃旧冲突类，统一新版）

原有 `.card` / `.card-hover` 全部废弃，统一使用新版玻璃化组件类，写入 index.css

## 4.1 卡片体系（核心升级）

css

```css
/* 基础玻璃卡片（全局默认） */
.card-glass {
  background: var(--bg-card);
  backdrop-filter: blur(16px);
  border: 1px solid var(--border-light);
  border-radius: 16px;
  padding: 16px;
  transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
}
/* 可点击交互卡片（首页快捷入口、单词列表项） */
.card-interactive {
  .card-glass;
}
.card-interactive:active {
  transform: scale(0.97);
  border-color: var(--border-strong);
  box-shadow: 0 0 12px var(--primary-glow);
}
.card-interactive:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(0,0,0,0.08);
}
/* 大尺寸模块卡片（今日学习、日历、个人中心数据块） */
.card-large {
  .card-glass;
  border-radius: 20px;
  padding: 20px;
}
/* 闪卡专属橙色玻璃卡片 */
.card-flash {
  background: var(--orange-bg);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(242, 112, 68, 0.2);
  border-radius: 20px;
}
/* 对话气泡卡片 */
.card-chat-ai {
  .card-glass;
  border-top-left-radius: 4px;
}
.card-chat-user {
  background: primary-600;
  color: white;
  border-top-right-radius: 4px;
  border: none;
}
```

## 4.2 按钮统一类（废弃零散 active 缩放）

css

```css
.btn-base {
  padding: 10px 18px;
  border-radius: 12px;
  font-weight: 500;
  transition: all 0.15s ease;
}
.btn-primary {
  .btn-base;
  background: primary-600;
  color: white;
}
.btn-primary:active {
  transform: scale(0.95);
  box-shadow: 0 0 10px var(--primary-glow);
}
.btn-success {
  .btn-base;
  background: success;
  color: white;
}
.btn-danger {
  .btn-base;
  background: danger;
  color: white;
}
.btn-text {
  .btn-base;
  background: transparent;
  color: primary-400;
}
```

## 4.3 装饰类优化（保留功能，柔和化）

1. `.accent-bar`：左侧竖线宽度改为 2px，透明度 0.6
2. `.progress-gradient`：渐变改为 `primary-600 → primary-400`，圆角 999px，高度统一 8px
3. `.glow-primary`：阴影扩散缩小，降低光晕强度，避免刺眼
4. `.pattern-dots`：Header 点阵透明度降至 6%，尺寸缩小，弱化装饰
5. `.quote-mark`：引号透明度改为 8%，缩小至 60px，不抢夺文字视觉重心

## 4.4 滚动、布局、动画统一

### 动画统一缓动与时长（全部替换原有分散配置）

css

```css
/* 页面切换入场 */
.page-enter {
  animation: pageSlide 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}
/* 卡片逐个入场（列表 stagger） */
.card-enter {
  animation: cardUp 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}
/* 闪卡翻转动画（替换旧opacity切换，新增3D翻转） */
.word-flip {
  perspective: 1000px;
}
.word-flip-inner {
  transition: transform 0.45s cubic-bezier(0.16,1,0.3,1);
  transform-style: preserve-3d;
}
.word-flip-flipped .word-flip-inner {
  transform: rotateY(180deg);
}
/* 流式光标闪烁 */
.streaming-cursor-bar {
  animation: blink 0.8s infinite;
}
/* 下拉菜单展开 */
.dropdown-open {
  animation: dropdownShow 0.3s ease-out;
```

### 废弃旧动画 / 交互类

- 删除 `.card-hover`、`.card-lift`、`.word-slide-left/right`，统一用 `card-interactive` + 新入场动画
- 旧闪卡 opacity 切换逻辑替换为 3D 翻转 CSS

# 五、组件视觉升级细则（所有 components 下文件改造标准）

## 5.1 BottomNav.tsx 底部导航（全局 4Tab）

1. 容器改用 `card-glass`，圆角 20px，上下轻微内边距，悬浮阴影
2. 选中 pill 滑动动画保持，pill 背景改为 `primary-600`，柔光 `glow-primary`
3. 图标统一 lucide 线性 24px，未选中：`text-hint`；选中：纯白
4. Tab 文字字号 12px，字重 500，选中白色
5. 非主页面 (wordlist/flashreview) 隐藏逻辑不变，样式同步适配暗色
6. 底部 home 指示条：背景 `primary-200`，圆角 999

## 5.2 StatusBar.tsx 手机状态栏

1. 文字统一 `text-main`，暗色自动切换，字号 12px
2. 信号 / 电池 SVG 线条粗细统一 1.5px，去除生硬黑色填充
3. 整体内边距 4px，弱化存在感，不抢夺头部视觉

## 5.3 Loader.tsx 加载动画

1. 方块跳跃尺寸缩小，光晕降低强度，颜色统一 `primary-400`
2. 新增全屏 / 卡片内两种尺寸 Loader，适配空页面、列表加载

## 5.4 ui/calendar.tsx 日历组件

1. 日历外层包裹 `card-large`
2. 今日日期外圈 `today-glow` 柔光弱化，填充底色 `primary-50`
3. 已学习日期圆点 `success`，周日文字 `danger`
4. 月份切换按钮使用 `.btn-text`，统一按压缩放
5. 展开 / 收起动画使用 `card-enter`，高度过渡柔和

# 六、六大页面逐页视觉升级规范（业务逻辑不动，仅改样式）

## 6.1 Home.tsx 首页（home Tab）

### 头部渐变 Header 改造

1. 渐变重制：亮色 `primary-200/0.3 → var(--bg-root)`；暗色 `#1A1133/0.4 → var(--bg-root)`
2. 保留 `.pattern-dots` 点阵装饰，透明度降低
3. 状态栏、欢迎文字、用户名、主题切换、头像全部套新排版层级
4. 暗色切换按钮使用 `.btn-text`，hover 发光反馈

### 今日学习卡片

1. 替换为 `card-large`，进度条使用 `.progress-gradient`
2. 数据数字 600 加粗，`text-main`；描述小字 `text-hint`

### 四大快捷入口（瞬間レビュー / 単語帳 / 学習時間 / 今日単語）

1. 全部使用 `card-interactive`，2 列网格布局，8px 间距
2. 图标 28px，主色填充；标题 16px，小字 12px
3. 右上角 `.card-action` 装饰圆透明度降至 10%，不突兀

### 日历模块

1. 外层 `card-large`，展开收起动画统一 `card-enter`
2. 日期格子 hover 轻微上浮，选中日期带浅紫底色

### 底部谚语区域

1. 引号 `.quote-mark` 弱化，日文大字 18px，释义 14px，标签使用小胶囊标签（surface 底色）

### 新增缺失规范

- 0 数据空状态：添加柔和占位插画 + 提示文字（`text-hint`）

## 6.2 CourseBrowser.tsx AI 对话页（course Tab）

1. 顶部 Header：玻璃卡片顶部栏，汉堡菜单按钮 `.btn-text`
2. AI 气泡：`card-chat-ai`，Markdown 文字排版统一行高 1.6
3. 用户气泡：`card-chat-user`，右对齐，无外边框
4. 流式输出光标闪烁动画保留，颜色改为白色 / 主色
5. 底部输入框容器：`card-glass`，输入框无边框，背景 `surface`
6. 发送按钮 `.btn-primary`，按压缩放反馈
7. 侧边对话历史列表：每一项使用 `card-interactive`，hover 高亮

## 6.3 WordDetail.tsx 文法練習（word Tab）

1. 顶部返回按钮 `.btn-text`，页面标题 24px
2. 顶层 Tab 切换器（助詞 / 変形 / 文型）：胶囊式标签，选中填充 `primary-600`，未选中 `surface`
3. 助词筛选标签：多选胶囊，选中主色浅底，取消 surface 底色
4. 子 Tab（比較 / 文中位置）缩小尺寸，弱化层级
5. 练习列表每一项：`card-interactive`，左侧 `.accent-bar` 紫色竖线，右侧箭头 hint 色
6. 列表 hover 轻微上浮，点击进入练习页保持原有跳转逻辑

## 6.4 WordList.tsx 単語リスト（无底部导航）

1. 书本 / 课程下拉菜单：玻璃容器，clip-path 动画保留，内部项 `card-interactive`
2. 进度条统一 `.progress-gradient`，文字数据加粗
3. 单词列表项：外层 `card-interactive`，左滑逻辑完全保留，仅修改滑动操作按钮样式
4. 左滑露出按钮：标记 (primary)、熟记 (success)、编辑 (hint)，圆角统一 12px
5. 单词汉字大字 18px，假名 16px，词性小字 12px，释义 `text-sub`
6. 切换书本 / 课程列表入场 stagger `card-enter` 延迟动画

## 6.5 FlashReview.tsx 瞬間レビュー（无底部导航）

1. 整体页面底色统一 `orangeBg`，所有容器使用 `card-flash`
2. 闪卡替换 3D 翻转动画（word-flip），废弃旧 opacity 切换
3. 正面：大号日文单词 32px；翻转后：假名 + 中文释义分层展示
4. 「覚えた」按钮 `.btn-success`；「忘れた」按钮 `.btn-danger`
5. 答对卡片短暂添加 success 浅底色，答错红色浅底色，0.8s 自动下一张逻辑不变
6. 学习状态统计卡片：横向均分数据，数字加粗突出

## 6.6 VocabularyGrid.tsx マイページ（vocab Tab）

1. 头像区域居中，头像圆形，外层主色柔光边框
2. 用户名称大标题 32px，学习标语 `text-sub`
3. 数据统计（単語 / 単語帳 / 連続日）横向均分，数字 600 加粗主色
4. 学習データ / 設定 / 表示模块：每个区块 `card-large`
5. 列表项使用 `card-interactive`，右侧箭头 hint 色
6. 暗色模式开关视觉优化，切换动画柔和

# 七、交互与动画统一改造清单

## 7.1 必须替换的旧实现

1. FlashReview 闪卡：opacity 显隐 → 3D CSS rotateY 翻转
2. 全局卡片 hover/active：废弃分散 scale，统一 `card-interactive` 类
3. 页面切换动画：统一 `page-enter`，移除 `word-slide-left/right`
4. 加载状态：统一 `Loader.tsx`，所有无数据 / 请求页面增加加载占位

## 7.2 新增缺失交互

1. 全部空页面（学习时间 0、单词 0、无对话历史）增加空状态视觉
2. 输入框聚焦时轻微主色边框发光
3. 下拉菜单子项 stagger 延迟入场动画（0.05s 递进）
4. 日历展开 / 收起高度平滑过渡

## 7.3 动画时长全局标准

- 微按压反馈：0.15s
- 卡片 hover / 页面入场：0.3s ~ 0.45s
- 3D 闪卡翻转：0.45s
- 下拉展开、日历切换：0.3s

# 八、代码改造约束（重要，防止破坏原有业务）

1. **禁止修改路由逻辑**：App.tsx 中 `useState` 路由切换、页面条件渲染逻辑完全不动
2. **禁止修改接口 / 状态**：CourseBrowser SSE 流式、单词数据、复习数组、日历日期数据硬编码保留
3. **禁止删除组件 / 页面文件**：所有 src 下目录、tsx 文件、data 数据文件完整保留，仅修改 className 与样式
4. **颜色替换规则**：全局搜索所有硬编码 Hex 色值，替换为 Tailwind 自定义语义色名
5. **className 替换规则**：旧 `.card` / `.card-hover` 全部替换为 `.card-glass` / `.card-interactive`
6. **主题切换**：原有 `onToggleDark` 暗色切换逻辑不变，仅补充 CSS 变量适配双模式
7. **移动端容器 .phone**：尺寸逻辑不变，仅修改背景色为 `var(--bg-root)`

# 九、验收标准（升级完成后核对）

1. 全页面亮色 / 暗色模式切换无色彩断层、无白色 / 深色突兀块
2. 所有卡片统一毛玻璃风格，圆角、间距、动效完全统一
3. 无任何硬编码 Hex 颜色，全部使用 CSS 变量 + Tailwind 自定义色
4. 闪卡 3D 翻转生效，原有点击翻牌业务逻辑不变
5. 所有页面入场、下拉、滑动、按压微交互统一缓动与时长
6. 日语文字层级清晰，大字单词突出，辅助文字弱化不抢视线
7. 橙色闪卡模块、紫色主界面、青色成功、红色错误色彩语义区分清晰
8. 空状态、加载状态全覆盖，无空白生硬界面
9. 底部导航、日历、AI 对话、单词列表、个人中心视觉风格统一，无割裂感
10. 原有全部业务功能（单词滑动、AI 流式对话、语法筛选、日历打卡、闪卡记忆标记）运行正常，无逻辑 bug